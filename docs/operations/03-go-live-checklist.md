# 上线检查清单 / 本番リリースチェックリスト

> 本番環境へのリリース前に確認すべき全項目
> 正式上线前需要确认的所有项目

---

## 1. 基础设施 / インフラストラクチャ

- [ ] SSL 证书已配置并验证 / SSL 証明書が設定済みで検証済み
- [ ] DNS 记录已配置（A 记录、CNAME）/ DNS レコード設定済み（A レコード、CNAME）
- [ ] Supabase 项目已创建（Pro 计划）/ Supabase プロジェクト作成済み（Pro プラン）
- [ ] Redis 实例已部署并可连接 / Redis インスタンスがデプロイ済みで接続可能
- [ ] CDN 已配置（静态资源）/ CDN 設定済み（静的アセット）
- [ ] 防火墙规则已配置 / ファイアウォールルール設定済み
- [ ] 负载均衡器已配置（如需要）/ ロードバランサー設定済み（必要に応じて）

---

## 2. 数据库 / データベース

- [ ] 所有迁移已应用 / 全マイグレーション適用済み
- [ ] 种子数据已加载（管理员账户、初始配置）/ シードデータ投入済み（管理者アカウント、初期設定）
- [ ] 索引已创建并验证 / インデックス作成済みで検証済み
- [ ] RLS（行级安全）策略已激活 / RLS（行レベルセキュリティ）ポリシー有効化済み
- [ ] tenant_id 分区策略已验证 / tenant_id パーティション戦略が検証済み
- [ ] 连接池配置已优化（pgBouncer）/ コネクションプール設定が最適化済み（pgBouncer）
- [ ] 数据库备份已配置并测试 / データベースバックアップが設定済みでテスト済み
- [ ] 数据库用户权限已正确设置 / データベースユーザー権限が正しく設定済み

---

## 3. 后端 / バックエンド

- [ ] 环境变量已设置 / 環境変数が設定済み:
  - [ ] `JWT_SECRET` — 强随机值 / 強ランダム値
  - [ ] `DATABASE_URL` — Supabase 连接字符串 / Supabase 接続文字列
  - [ ] `REDIS_URL` — Redis 连接字符串 / Redis 接続文字列
  - [ ] `SUPABASE_URL` — Supabase API URL
  - [ ] `SUPABASE_SERVICE_KEY` — Supabase サービスキー
  - [ ] `B2_CLOUD_*` — B2 Cloud 认证信息 / B2 Cloud 認証情報
  - [ ] `SENTRY_DSN` — エラートラッキング DSN
  - [ ] `NODE_ENV=production`
- [ ] 健康检查端点正常响应 / ヘルスチェックエンドポイントが正常応答
  - [ ] `GET /api/health` → 200
  - [ ] `GET /api/health/db` → 200
  - [ ] `GET /api/health/redis` → 200
- [ ] 速率限制已配置 / レート制限が設定済み
- [ ] CORS 已正确配置 / CORS が正しく設定済み
- [ ] 请求日志（Pino）已配置 / リクエストログ（Pino）が設定済み
- [ ] Graceful shutdown 已实现 / Graceful shutdown が実装済み

---

## 4. 前端 / フロントエンド

- [ ] API URL 已配置为生产环境 / API URL が本番環境に設定済み
- [ ] 构建成功（无警告、无错误）/ ビルド成功（警告なし、エラーなし）
- [ ] 静态资源已启用缓存（Cache-Control）/ 静的アセットのキャッシュ有効化済み（Cache-Control）
- [ ] Source map 已在生产构建中禁用 / Source map が本番ビルドで無効化済み
- [ ] 环境变量已注入 / 環境変数が注入済み
- [ ] Favicon 和 meta 标签已更新 / Favicon と meta タグが更新済み

---

## 5. 安全 / セキュリティ

- [ ] OWASP Top 10 扫描已通过 / OWASP Top 10 スキャンが通過
- [ ] 依赖审计无高危漏洞 / 依存関係監査で高危険度脆弱性なし
  ```bash
  npm audit --production
  ```
- [ ] 代码中无硬编码密钥 / コード内にハードコードされた秘密情報なし
- [ ] Helmet.js 已启用 / Helmet.js が有効化済み
- [ ] CSRF 保护已启用 / CSRF 保護が有効化済み
- [ ] SQL 注入防护已验证（参数化查询）/ SQL インジェクション対策が検証済み（パラメータ化クエリ）
- [ ] XSS 防护已验证（输入消毒）/ XSS 対策が検証済み（入力サニタイズ）
- [ ] JWT token 有效期合理 / JWT トークン有効期限が適切
- [ ] 密码策略已配置 / パスワードポリシーが設定済み

---

## 6. 监控与告警 / モニタリングとアラート

- [ ] 健康检查监控已配置 / ヘルスチェック監視が設定済み
- [ ] 告警规则已激活 / アラートルールが有効化済み:
  - [ ] API 5xx 错误率 > 1% / API 5xx エラー率 > 1%
  - [ ] API 响应时间 p95 > 500ms / API レスポンスタイム p95 > 500ms
  - [ ] 数据库连接池使用率 > 80% / DB コネクションプール使用率 > 80%
  - [ ] 磁盘使用率 > 85% / ディスク使用率 > 85%
  - [ ] Redis 内存使用率 > 80% / Redis メモリ使用率 > 80%
- [ ] Sentry 错误追踪已连接 / Sentry エラートラッキング接続済み
- [ ] 日志聚合已配置 / ログ集約が設定済み

---

## 7. 数据迁移 / データ移行

- [ ] MongoDB → PostgreSQL 迁移脚本已执行 / MongoDB → PostgreSQL 移行スクリプト実行済み
- [ ] 行数对比验证（差异 < 0.1%）/ 行数比較検証（差異 < 0.1%）
- [ ] 数据校验和匹配 / データチェックサム一致
- [ ] 关键业务数据抽样验证 / 重要業務データのサンプリング検証
  - [ ] 产品数据 / 商品データ
  - [ ] 库存数据 / 在庫データ
  - [ ] 订单数据 / 注文データ
  - [ ] 用户数据 / ユーザーデータ
- [ ] 多租户数据隔离验证 / マルチテナントデータ分離検証

---

## 8. 测试 / テスト

- [ ] 全部单元测试通过 / 全ユニットテスト通過
- [ ] 全部集成测试通过 / 全結合テスト通過
- [ ] E2E 关键路径已验证 / E2E クリティカルパスが検証済み:
  - [ ] 登录 → 仪表板 / ログイン → ダッシュボード
  - [ ] 入库创建 → 库存更新 / 入庫作成 → 在庫更新
  - [ ] 出库创建 → B2 Cloud 连携 / 出庫作成 → B2 Cloud 連携
  - [ ] 用户管理（CRUD）/ ユーザー管理（CRUD）
- [ ] 负载测试完成（目标 100 并发）/ 負荷テスト完了（目標 100 同時接続）
- [ ] 测试覆盖率 ≥ 80% / テストカバレッジ ≥ 80%

---

## 9. 文档 / ドキュメント

- [ ] API 文档（Swagger/OpenAPI）已更新 / API ドキュメント（Swagger/OpenAPI）更新済み
- [ ] 运维手册已审查 / 運用マニュアルがレビュー済み
- [ ] 灾难恢复计划已审查 / DR 計画がレビュー済み
- [ ] 故障排查指南已就位 / トラブルシューティングガイドが整備済み

---

## 10. 回滚 / ロールバック

- [ ] 回滚流程已文档化 / ロールバック手順がドキュメント化済み
- [ ] 回滚已在 staging 环境测试 / ロールバックが staging 環境でテスト済み
- [ ] 数据库回滚脚本已准备 / データベースロールバックスクリプトが準備済み
- [ ] 前一版本的 Docker 镜像可用 / 前バージョンの Docker イメージが利用可能

---

## 签核 / サインオフ

| 角色 / 役割 | 签核人 / サインオフ者 | 日期 / 日付 |
|---|---|---|
| 开发负责人 / 開発リード | | |
| 运维负责人 / 運用リード | | |
| 安全负责人 / セキュリティリード | | |
| 产品负责人 / プロダクトオーナー | | |
