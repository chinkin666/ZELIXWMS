> **状态: 远期目标** — 日均10万单的架构设计。当前优先级是跑通第一个客户（Phase 0-1）。当业务量接近日均1万单时开始引入 Redis 缓存 + 消息队列。10万单级别再考虑完整微服务改造。
> **ステータス: 長期目標** — 日次10万注文のアーキテクチャ。現在の優先事項は最初の顧客を通すこと（Phase 0-1）。日次1万注文に近づいた時点で Redis + MQ を導入。10万注文レベルで完全マイクロサービス化を検討。

《ZELIX WMS 高并发架构（10万订单 / 天）》。
这种架构主要解决三个核心问题：

订单并发

库存一致性

任务处理吞吐量

很多大型仓储系统（例如 SAP Extended Warehouse Management 或 Manhattan WMS）在订单量上来之后都会采用类似的设计。

一、ZELIX WMS 高并发总体架构
                    Clients
            Web / PDA / API / Marketplace

                           │
                           ▼
                     API Gateway
                  (Auth / RateLimit)

                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
      Order Service   Inventory Service   Warehouse Service
         订单服务           库存服务             仓库服务

                           │
                           ▼
                       Event Bus
                     (Kafka / MQ)

                           │
     ┌──────────────┬──────────────┬──────────────┐
     ▼              ▼              ▼              ▼
Shipment Worker  Picking Worker  Inventory Worker  Notification Worker
   出荷任务          拣货任务         库存处理           通知任务
二、高并发系统关键组件

系统核心组件：

组件	作用
API Gateway	请求入口
Event Bus	事件系统
Worker	后台任务
Cache	高速缓存
Database	数据存储
三、订单高并发处理

订单来源：

Rakuten
Shopify
API
Batch Import

订单进入流程：

Order Request
 ↓
API Gateway
 ↓
Order Service
 ↓
Event Bus
 ↓
Create Shipment

订单写入数据库后：

触发事件

例如：

order.created
四、异步任务架构

为了避免 API 阻塞：

所有重任务异步

流程：

Order Created
 ↓
Event Bus
 ↓
Create Shipment Worker
 ↓
Create Wave Worker
 ↓
Create Pick Tasks

这样：

API 可以 10ms 返回
五、库存高并发设计

库存是 WMS 最容易出问题的地方。

必须避免：

超卖

解决方法：

1 库存锁
inventory_reservation

流程：

订单
 ↓
库存锁定
 ↓
生成出荷
2 原子更新

数据库更新：

available - quantity
reserved + quantity

必须：

atomic
3 Redis库存缓存

热点库存：

Redis

减少数据库压力。

六、任务队列设计

任务必须使用队列。

队列：

order_queue
shipment_queue
picking_queue
inventory_queue

任务流程：

订单
 ↓
Queue
 ↓
Worker
 ↓
执行任务
七、Worker架构

Worker专门处理任务。

Worker Cluster

├ Order Worker
├ Shipment Worker
├ Inventory Worker
├ Picking Worker
└ Notification Worker

每个 Worker：

水平扩展

例如：

10 个 Picking Worker
八、库存更新流程

库存变化流程：

Pick Completed
 ↓
Shipment Ready
 ↓
Inventory Deduction
 ↓
Inventory Ledger

库存流水：

inventory_ledger

保证：

库存可追溯
九、数据库分片

订单量大时：

MongoDB Sharding

分片键：

tenantId
warehouseId

订单表：

orders

按：

createdAt

分片。

十、缓存架构

缓存层：

Redis Cluster

缓存数据：

SKU
库存
规则
用户权限

减少数据库压力。

十一、读写分离

数据库：

Primary
Replica

写：

Primary

读：

Replica

用于：

报表
查询
十二、系统吞吐能力

一个合理架构可以支持：

指标	规模
订单 / 天	100k
库存记录	10M
库存流水	100M
十三、WMS 高并发核心原则

真正高并发系统遵守：

API 不做重任务

而是：

API
 ↓
Event
 ↓
Worker
十四、ZELIX WMS 高并发核心组件
API Gateway
Event Bus
Queue
Worker
Redis
MongoDB
十五、完整高并发架构
Client
 │
API Gateway
 │
Services
 │
Event Bus
 │
Queues
 │
Workers
 │
MongoDB
Redis
Object Storage
十六、真正的高并发瓶颈

WMS 最大瓶颈通常是：

库存锁

如果设计不好：

库存冲突

系统会崩。

💡 最后给你一个关键建议

真正成熟的 WMS 在高并发下必须做到：

库存计算 = 库存流水

而不是：

直接改库存

否则：

库存会乱