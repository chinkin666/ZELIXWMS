# ============================================================
# Nginx Reverse Proxy / リバースプロキシ / 反向代理
# ============================================================
FROM nginx:1.27-alpine

# デフォルト設定を置換 / 替换默认配置
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80 443
